#include "hfile.h"

int main(int argc, char const *argv[])
{
    cout << "Hello world!";
    unique_ptr<mobdata> orc1(new orc);
    unique_ptr<mobdata> orc2(new orc);
    //orc1->attackfunc(orc2);
    return 0;
}
